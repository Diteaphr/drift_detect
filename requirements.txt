# Core — required for all experiments
numpy>=1.24,<3
pandas>=1.5
scikit-learn>=1.0
scipy>=1.7.0
river>=0.21
matplotlib>=3.5

# Required for Type-LDD FAN ProtoNet (src/type_ldd) and optional model_type='gru'
torch>=2.0

# Optional — only needed for model_type='xgb'
# xgboost>=2.0

# Live monitor / Streamlit Cloud deployment (streamlit run monitor_app.py)
# monitor_app.py uses width="stretch"; verified working on 1.61.
streamlit>=1.61
altair>=5.0
